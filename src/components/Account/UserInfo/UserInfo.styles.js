import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 16,
    paddingTop: 80,
    paddingBottom: 20,
    backgroundColor: "#fff",
  },
  name: {
    fontSize: 35,
    fontWeight: "bold",
  },
  email: {
    color: "#003087",
    fontWeight: "600",
    fontSize: 20,
  },
});
