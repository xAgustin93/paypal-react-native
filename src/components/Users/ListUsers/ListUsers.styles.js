import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    fontWeight: "bold",
    fontSize: 18,
    paddingBottom: 10,
  },
  listUsers: {
    paddingBottom: 350,
  },
});
