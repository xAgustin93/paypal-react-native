import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    fontWeight: "500",
    fontSize: 30,
    marginBottom: 16,
  },
  subtitle: {
    fontSize: 20,
    marginBottom: 16,
  },
});
